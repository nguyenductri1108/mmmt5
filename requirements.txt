# Core
pandas>=2.0
numpy>=1.24
PyYAML>=6.0
python-dotenv>=1.0
requests>=2.31

# Broker (Windows only — the package does not build on macOS/Linux)
MetaTrader5>=5.0.45 ; sys_platform == "win32"

# AI layer (both are optional at runtime; the bot runs without them)
anthropic>=0.60
openai>=1.40
